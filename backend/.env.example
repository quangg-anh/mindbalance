# Exact frontend origins, comma-separated. No wildcard and no trailing path.
CORS_ORIGINS=https://<FRONTEND_HOST>
PORT=8787
NODE_ENV=production
SAVE_STORE=file
# Must point into mounted persistent disk.
SAVE_STORE_FILE=/data/saves.json
