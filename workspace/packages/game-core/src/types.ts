import type { Effects } from '@game/game-content';
export interface GameState { schemaVersion:number; seed:number; rngState:number; rngVersion:'mulberry32-v1'; month:number; actionPoints:number; stats:Record<'health'|'spirit'|'knowledge'|'skill'|'morality'|'money'|'debt',number>; relationships:Record<string,number>; traits:Record<string,number>; flags:Record<string,boolean>; history:string[]; eventRuntime:Record<string,{lastMonth:number;times:number}>; pendingEvent?:string; delayed:Array<{dueMonth:number;effects:Effects}>; ending?:string }
export type Command={type:'START_GAME';seed:number}|{type:'SELECT_ACTIVITY';activityId:string}|{type:'CHOOSE_EVENT_OPTION';eventId:string;choiceId:string}|{type:'ADVANCE_MONTH'}|{type:'CONFIRM_STOP_JOURNEY'}|{type:'RESTORE_SAVE';state:GameState};
